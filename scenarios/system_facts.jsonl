{"id": "sf01-date-fact", "category": "system_facts", "severity": "regression", "tags": ["date", "locale"], "tools": [{"name": "schedule_meeting", "description": "schedule a meeting", "parameters": {"type": "object", "properties": {"title": {"type": "string"}, "when": {"type": "string", "description": "absolute ISO datetime"}}, "required": ["title", "when"]}}], "system": "date: 2026-07-21 Tue 14:30; locale: en-US; device: phone; battery: 62%", "prompt": "set up a sync with the team tomorrow at 7pm", "expect": {"kind": "call", "name": "schedule_meeting"}, "min_confidence": 0.2}
{"id": "sf02-no-fact", "category": "system_facts", "severity": "smoke", "tags": ["no-fact"], "tools": [{"name": "schedule_meeting", "description": "schedule a meeting", "parameters": {"type": "object", "properties": {"title": {"type": "string"}, "when": {"type": "string"}}, "required": ["title", "when"]}}], "prompt": "find time for a chat next tuesday", "expect": {"kind": "any"}}
{"id": "sf03-assistant-fact", "category": "system_facts", "severity": "edge", "tags": ["identity"], "tools": [{"name": "intro", "description": "When the user asks the assistant to introduce itself or asks 'what's your name', emit this call exactly once with the assistant's name (taken from the system facts).", "parameters": {"type": "object", "properties": {"name": {"type": "string", "description": "assistant name"}}, "required": ["name"]}}], "system": "assistant: Friday", "prompt": "Hi, what should I call you?", "expect": {"kind": "any", "min_confidence": 0.0}}
